# Config default -- usado quando ./webserv roda sem argumento nenhum.
# Caminho relativo de propósito: precisa funcionar em qualquer máquina,
# rodando a partir da raiz do projeto ("./webserv").
server {
	listen 8080;
	root ./config;

	location / {
		methods GET;
		index index.html;
	}
}
